# 服务器列表页（`/servers`）实施计划

> **状态：未开工。** 2026-09-14 讨论定案，用户拍板「先不做，把计划记下来」。
> **触发条件**：有服主主动来问「能不能在你们站上列出我们的服」——现在做大概率是空列表（参考 Cobblemon 那个页面能撑起来是因为它有几万用户）。

## 目标

给装了 CobbleMarket 的服务器一个**公开露出位**：服主提交服务器信息，网站 `/servers` 页列出，玩家据此找服。

**参考实现**：<https://cobblemon.com/en/servers>

## 参考页的结构（已核对截图）

- **顶部**：标题 + 说明（社区驱动、官方无服务器、**顺序每次访问随机以保证公平**）+ 提交入口提示
- **左侧筛选栏**：搜索框 / 平台（Fabric·NeoForge）/ 语言（国旗）/ 玩法（Adventure·Creative·Peaceful·Survival）/ 游戏机制（Hostile Pokémon·PvP·Gyms·Roleplay·Crates…）
- **主区**：分页（`1 of 23`）+ 服务器卡片
- **卡片**：左＝图标/横幅 + 在线状态与人数（离线灰点、在线绿点 `2 / 40`）；右＝名称 + 简介 + 地址（**带复制按钮**）+ Discord / 官网图标 + 标签胶囊

## 我们的方案

**提交入口**：GitHub Issue 模板（**不用 Discord** —— 更适合我们这体量，且能自动转数据、不用人肉维护）。
**数据流**：`Issue（模板表单）` → 打上 `approved` label → GitHub Action 转成 `docs/servers.json` → `/servers` 页 JS 读取渲染。

**零模组改动**，全在网站仓库。

### 字段

| 字段 | 说明 | 必填 |
|---|---|---|
| 服务器名 | | ✓ |
| 地址 | `play.example.com:25565` | ✓ |
| 版本 | Minecraft 1.21.1 / Cobblemon 1.8 | ✓ |
| 简介 | 一两句 | ✓ |
| 图标 / 横幅 | 图片 URL | |
| 链接 | Discord / QQ 群 / 官网 | |
| 标签 | 见下 | |
| 联系方式 | 申请人 GitHub 用户名（仅用于沟通） | ✓ |

### 标签集（**草案，待用户最终确认**）

Cobblemon 那套（平台/语言/玩法/游戏机制）对我们太全，建议精简：

- **语言**：中文 / 英文 / 多语言
- **平台**：Fabric / NeoForge
- **玩法**：生存 / 创造 / 经济 / PvP / RPG / 休闲

### 展示

- `/servers` + `en/servers` 两页，**卡片横条**（照参考页的排版：左图标、右信息）
- 卡片视觉**复用鸣谢名单那套**（像素边框 + 实心偏移阴影 + 悬停抬起），与站点是一套东西
- 侧边栏加入口（`_sidebar.md` 和 `en/_sidebar.md` **都要加**）
- **顺序随机**（每次进页面打乱）—— 学参考页，避免「谁先提交谁永远在前」的不公平感

## 分两期

### 第一期（核心）
1. `.github/ISSUE_TEMPLATE/server-submission.yml` —— 表单模板
2. `.github/workflows/build-server-list.yml` —— label 触发 → 解析 Issue → 写 `docs/servers.json` → 提交
3. `docs/servers.md` + `en/servers.md` + 一段 JS 渲染（卡片、标签、搜索、筛选、复制地址、随机排序）
4. 侧边栏入口 + 首页/`features.md` 加一句引导

### 第二期（锦上添花）
- **在线状态与人数**：Action 定时 ping 服务器地址（如每 10 分钟）写进 JSON，页面显示绿点/灰点与「在线/上限」。
  ⚠ **不是实时的**（受抓取频率限制），页面上要如实说明，别让玩家误以为实时。

## 开工前要问用户的

1. **标签集**最终版（上面是草案）
2. **排序**：随机 vs 先来后到
3. **在线人数**是否第一期就带
4. **审核标准**：要不要限定「必须装了 CobbleMarket」之类

## 别忘的坑

- 网站仓库**可以直接提交推送**（与模组仓库相反，那边必须等用户开口）
- 新增页面必须**中英各一份**，并**同时改两份 `_sidebar.md`**（见 `CLAUDE.md` 第三节）
- docsify 里读 JSON 用**相对路径**（`servers.json`），别用 `/servers.json`（英文站会 404）
- 卡片里的名称/简介来自用户提交，**必须 HTML 转义**（同鸣谢名单的 XSS 处理）
- 卡片标题用 `<div>`，**不要用 `<h3>`**（会被吸进侧边栏目录）
